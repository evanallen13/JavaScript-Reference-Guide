let day1 = {
    squirrel: false,
    events: ["work","touch tree",'pizza','running'],
}

//console.log(day1.squirrel)

let anObject = {left: 1, right:2 }

//log('left' in anObject) in operator spits out a bolean values if method is in function

// Assigning new properties 
Object.assign(anObject, {up: 3, down: 4})
console.log(anObject)

